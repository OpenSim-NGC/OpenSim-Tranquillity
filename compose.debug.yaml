
services:
  opensimserverregionserver:
    image: opensimserverregionserver
    build:
      context: .
      dockerfile: Source/OpenSim.Server.RegionServer/Dockerfile
      args:
        - configuration=Debug
    volumes:
      - ~/.vsdbg:/remote_debugger:rw
